console.log("Kairis Proxmox test deployment");
console.log("1. Copy .env.proxmox-test.example to .env.proxmox-test if needed.");
console.log("2. Fill Supabase values when available.");
console.log("3. Keep EXCHANGE_PROVIDER=mock and ENABLE_LIVE_ASSISTED_TRADING=false for initial tests.");
console.log("4. Run: docker compose -f docker-compose.proxmox-test.yml up -d --build");
